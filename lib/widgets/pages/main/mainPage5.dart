import 'package:flutter/material.dart';

class MainPage5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('MainPage5'),
        ),
      ),
    );
  }
}
